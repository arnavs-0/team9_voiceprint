# Watermarked Voice Authentication System

## Command Line Interface

1. Run the model directly:
   ```
   python watermarked.py
   ```

Same functionality as model.py in multi_user, but with watermarking.